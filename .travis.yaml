before_install:
 - "docker build -t jonathanzea/docker-react -f Dockerfile.dev ."
language: generic
script:
 - "docker run -e CI=true jonathanzea/docker-react npm run test"
services: 
 - docker
sudo: required